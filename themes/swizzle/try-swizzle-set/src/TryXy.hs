{-# LANGUAGE TemplateHaskell #-}

module TryXy where

import Data.Swizzle.Set.TH
-- import Data.Swizzle.Set.Class.Base

-- swizzleSet "xy"

-- xy :: SwizzleSet2 a => (X a, Y a) -> a -> a
-- xy (v, w) = x v . y w
