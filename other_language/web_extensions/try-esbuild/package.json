{
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
	  "build": "./node_modules/.bin/esbuild src/tryEsbuild.js --bundle --outfile=dist/tryEsbuild.js",
	  "run": "npm run build && web-ext run --source-dir dist",
	  "find-android": "web-ext run --source-dir dist -t firefox-android",
	  "android": "npm run build && web-ext run --source-dir dist -t firefox-android --android-device=$npm_config_android_device --firefox-apk=org.mozilla.fenix",
	  "package": "npm run build && web-ext build --source-dir dist"
  }
}
